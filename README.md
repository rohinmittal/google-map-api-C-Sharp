Download the code and make following change:
a) In googleAPI/googleAPI.cs file, update the API key:
public static string _apiKey = "<api_key>";

b) apiRequest.cs requires following two input arguments:
"<address>" "<placeType>"
